# Attributes
attributes:
  v1:
  - name: realm
    description: Realm is the authentication realm.
    type: enum
    exposed: true
    required: true
    allowed_choices:
    - AWSIdentityDocument
    - AWSSecurityToken
    - Certificate
    - Google
    - LDAP
    - Vince
    - GCPIdentityToken
    - AzureIdentityToken
    - OIDC
    example_value: Certificate
