platform: linux

image_resource:
  type: docker-image
  source:
    repository: gcr.io/aporetodev/domingo

inputs:
- name: src
  path: go/src/github.com/aporeto-inc/gaia

run:
  path: bash
  args:
    - -ec
    - |
      export PATH=$PATH:$GOPATH/bin:$PWD/go/bin
      export GOPATH=$PWD/go:$GOPATH

      cd go/src/github.com/aporeto-inc/gaia
      dep ensure -v
      ./.test.sh
